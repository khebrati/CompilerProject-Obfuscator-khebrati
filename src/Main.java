import gen.MinicLexer;
import gen.MinicParser;
import org.antlr.v4.runtime.CharStreams;
import org.antlr.v4.runtime.CommonTokenStream;

public static void main(String[] args) {
//    // Setup lexer and parser
//    MinicLexer lexer = new MinicLexer(CharStreams.fromString(sourceCode));
//    CommonTokenStream tokens = new CommonTokenStream(lexer);
//    MinicParser parser = new MinicParser(tokens);
//    MinicParser.ProgramContext tree = parser.program();
//
//    // Apply Minication
//    String MinicatedCode = VariableMinicator.Minicate(tree, tokens);
//    System.out.println(MinicatedCode);
}